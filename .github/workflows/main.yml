on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4

    - name: 📂 Sync files
      run: |
        sudo apt-get update
        sudo apt-get install -y sshpass rsync
        sshpass -p 'AmiMotiur27@' rsync -avzr --delete --exclude-from='.rsyncignore' -e "ssh -o StrictHostKeyChecking=no -p 22" ./ onenetly@158.69.60.81:/home/onenetly/htdocs/onenetly.com/

